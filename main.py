from recolha import *
from valores_em_falta import *
from outliers import *

def main():

    dados = ler_ficheiro(path_name)
    if dados:
        print("Ficheiro lido com sucesso!")
    else:
        print("Não foi possível carregar os dados.")
        return

    assert  is not None and dados != ""
    assert  is not None and dados != ""
    assert  is not None and dados != ""

    assert  is not None and dados != ""
    assert  is not None and dados != ""
    assert  is not None and dados != ""
    assert  is not None and dados != ""

    assert  is not None and dados != ""

if __name__ == "__main__":
    main()